============
Installation
============

At the command line::

    $ easy_install dj-elasticsearch-flex

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-elasticsearch-flex
    $ pip install dj-elasticsearch-flex
